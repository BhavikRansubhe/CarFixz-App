<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br14x.carfixz">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".BookingDetails" />
        <activity android:name=".DisplayBookings" />
        <activity android:name=".OrderDetails" />
        <activity android:name=".DisplayOrders" />
        <activity android:name=".SaiSerViceCenterApptBooking_1" />
        <activity android:name=".SaiSerViceCenterApptBooking" />
        <activity android:name=".SaiServiceIntro" />
        <activity android:name=".RoadsideAssistance" />
        <activity android:name=".Appt_Booking_final" />
        <activity android:name=".OmServiceCenterApptBooking_1" />
        <activity android:name=".OMServiceCenterIntro" />
        <activity android:name=".A1garageApptBooking_1" />
        <activity android:name=".A1garageIntro" />
        <activity android:name=".views.Shop" />
        <activity android:name=".searchActivity" />
        <activity android:name=".car_accessories_shop" />
        <activity android:name=".OmSerViceCenterApptBooking" />
        <activity android:name=".A1garageApptBooking" />
        <activity android:name=".garage_view" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
    </application>

</manifest>